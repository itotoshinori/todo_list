<% if @blog.present? %>
    <% @blog.each do |f| %>
        <h2 class="page_title"><%= f.title %></h2>
        <%=simple_format(f.content) %>
        <%= link_to "修正","/blogs/#{f.id.to_s}/edit" %>
    <% end %>
<% end %>
