<h1>ユーザー一覧</h1>
<table>
  <tr>
    <th scope="col" style="width:140px">名前</th>
    <th>メールアドレス</th>
  </tr>
<% @users.each do |user|%>
  <tr>
    <td><%= user.name %></td>
    <td><%= user.email %></td>
    <td><%= link_to "delete", user_path(user.id) , method: :delete,data: { confirm: "本当に削除しますか？?" } %></td>
  </tr>
<% end %>
</table>


