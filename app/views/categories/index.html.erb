<h1>分類別</h1>
<% @category_ids.each do |category,count,unfinish_count| %>
  <h2><%= link_to category, "/categories/index_detail/?category=#{category}" %>(<%= unfinish_count %>/<%= count %>)</h2>
<% end %> 
<hr />    
